        <header>
          <h1>
{% if article.active %}
            {{ article.title }}
{% else %}
            <a href="{{ article.url }}">{{ article.title }}</a>
{% endif %}
          </h1>
{% if article.header.subtitle %}
          <h2>{{ article.header.subtitle }}</h2>
{% endif %}
{% if article.header.date %}
          <h2>{{ article.header.date|date("F d, Y") }}</h2>
{% endif %}
        </header>
