    <header>
      <div class="logo">
        <a href="{{ home_url }}" >
{% if config.site.logo %}
          {{ media['image://' ~  config.site.logo ].html() }}
{% endif %}
          <h1>{{ site.title }}</h1>
{% if theme.subtitle  %}
          <h2>{{ theme.subtitle }}</h2>
{% endif %}
        </a>
      </div>
{%  set menu_header = page.collection({'items':{'@taxonomy':{'photon': 'header'}},'order': {'by': 'default', 'dir': 'asc'}}) %}
{% include '_sections/nav.html.twig' with {'nav_list':menu_header} %}
{% include '_asides/motto.html.twig' %}
    </header>
