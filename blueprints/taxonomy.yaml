title: Taxonomy
@extends:
    type: article
    context: blueprints://pages

form:
  fields:
    tabs:
      fields:
        taxonomy:
          type: tab
          title: Taxonomy Collection
          fields:
            header.content.items:
              type: text
              label: Collection
              default: '@root.descendents'
